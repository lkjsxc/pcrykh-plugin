# Defaults

These are normative default values for a fresh rebuild.

- `commands.root: pcrykh`
- `gui.theme: classic`
- `runtime.chat.announce_achievements: true`
- `runtime.chat.tips_enabled: true`
- `runtime.chat.tips_interval_seconds: 120`
